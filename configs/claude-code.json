{
  "_comment": "Скопируйте нужный вариант в .claude/settings.json вашего проекта или ~/.claude/settings.json",

  "_variant_1_uvx_github": {
    "description": "Установка прямо из GitHub через uvx (рекомендуется)",
    "mcpServers": {
      "1c-price": {
        "command": "uvx",
        "args": [
          "--from", "git+https://github.com/snn02/1c-products-price",
          "mcp-1c-price"
        ]
      }
    }
  },

  "_variant_2_installed": {
    "description": "Если пакет уже установлен через pip/uv install",
    "mcpServers": {
      "1c-price": {
        "command": "mcp-1c-price"
      }
    }
  },

  "_variant_3_uv_run": {
    "description": "Локальный запуск через uv без глобальной установки",
    "mcpServers": {
      "1c-price": {
        "command": "uv",
        "args": [
          "run",
          "--directory", "/абсолютный/путь/до/1c-products-price",
          "mcp-1c-price"
        ]
      }
    }
  }
}
